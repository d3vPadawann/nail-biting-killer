readme_content = """# 🖐️ nail-biting-killer

Um aplicativo em Python desenhado para bloquear roer as unhas, utilizando Visão Computacional para monitorar e alertar quando a mão se aproxima da boca.

```bash
sudo apt update
sudo apt install python3-tk
```
